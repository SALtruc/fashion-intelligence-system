# datasets — share via Drive, too big for git
datasets/**/*.csv
datasets/**/*.jpg

# preprocessed_datasets is tracked on purpose. The manifests define the splits every
# notebook reads, so a fresh checkout has to land on the same rows without re-running
# the EDA notebook first. 16 MB of text, and no images are written here.

# artifacts — share via Drive, too big for git
artifacts/**
!artifacts/**/
!artifacts/**/.gitkeep
!artifacts/**/README.md

# the Colab data bundle -- 590 MB, rebuilt from the checkout by scripts/make_colab_bundle.py
task1_colab_data.zip

# junk
__MACOSX/
.DS_Store
Thumbs.db
.ipynb_checkpoints/
__pycache__/
*.pyc
.venv/
venv/
datasets.zip

# Full Kaggle bundle, rebuilt with --include-models
task1_collab_full.zip
